<%= form_with(model: special_rule, local: true) do |form| %>
  <% if special_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_rule.errors.count, "error") %> prohibited this special_rule from being saved:</h2>

      <ul>
      <% special_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ruleholder_id %>
    <%= form.number_field :ruleholder_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
